# Секреты — никогда не коммитить
.env
google_oauth_client.json
google_calendar_token.json
google_gmail_token_*.json

# Локальное состояние — какие письма уже показали в алертах, накопленные
# правила срочности (не секреты, но машинно-специфичны и бессмысленны в git)
seen_mail_ids.json
urgency_rules.json

# .env.example не содержит секретов (только имена переменных) — исключение
# из родительского правила .env.* в .gitignore репозитория project1-clone
!.env.example

# Python
__pycache__/
*.pyc
.venv/
venv/

# Временные голосовые файлы, скачанные для распознавания
*.ogg
*.mp3
tmp/
